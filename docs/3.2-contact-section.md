# 3.2 — Sekcja: `contact`

## Źródło

`routers/Contact/contact.py`, `schemas.py`

## Endpointy obecne

| Metoda | Path | Auth | Uwaga |
|---|---|---|---|
| POST | `/contact/contact/add_message` | brak (publiczny formularz) | zapis plików na dysk z nazwą `{random.random()}-{filename}` |
| DELETE | `/contact/contact/delete_message` | JWT | |
| GET | `/contact/contact/get_messages` | brak | **do przemyślenia — czy lista wiadomości kontaktowych nie powinna być za auth?** |
| POST | `/contact/contact/get_message` | JWT | |

## Problemy do naprawienia przy migracji

- [ ] `GET /get_messages` bez auth — publiczne ujawnianie listy wiadomości
      kontaktowych wygląda na błąd, nie feature — potwierdzić z Tobą
- [ ] Upload plików: brak walidacji rozszerzenia/rozmiaru/typu MIME, nazwa
      pliku budowana z `random.random()` (kolizje, brak sanityzacji
      `filename` → potencjalny path traversal jeśli filename zawiera `../`)
- [ ] `except (FileNotFoundError, Exception)` połyka wszystko — brak realnej
      obsługi błędów / logowania

## Docelowe pliki

- [ ] `database/psql/models/contact.py` (+ FK `images.id_message → contact.id`)
- [ ] `core/repository/psql/contact/{create,collection,one,delete}.py`
- [ ] `core/handler/contact/{action}.py`
- [ ] `core/process/contact/` — walidacja/sanityzacja uploadu (jeśli
      zostajemy przy zapisie na dysk lokalny, a nie np. S3)
- [ ] `api/schemas/contact/`
- [ ] `api/endpoints/{public|admin}/contact/{action}.py`
- [ ] Testy lustrzane

## Status

- [ ] Nie rozpoczęte — dobry kandydat na "pierwszy pełny przelot" wzorca,
      bo to najmniejsza domena
